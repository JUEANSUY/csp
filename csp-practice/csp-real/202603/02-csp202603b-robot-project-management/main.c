#include <stdio.h>

int main(void) {
    int n;
    int m;

    if (scanf("%d %d", &n, &m) != 2) {
        return 0;
    }

    for (int i = 0; i < m; i++) {
        int a;
        int b;
        int c;
        scanf("%d %d %d", &a, &b, &c);
    }

    return 0;
}
