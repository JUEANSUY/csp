# sample-sum

## 题目信息

- 类型：输入输出
- 目标：读入 n 个整数，输出它们的和。

## 思路

用循环读入每个数，把它们加到 `sum` 里。

## 关键点

- 求和可能超过 `int`，所以 `sum` 和每个输入数用 `long long`。
- `scanf("%lld", &x)` 对应 `long long`。

## 错误记录

- 暂无。

## 复杂度

- 时间复杂度：O(n)
- 空间复杂度：O(1)

## 复盘

这道题主要用来测试本地编译、运行和输入重定向流程。
